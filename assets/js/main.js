(async () => {
    const profileData = await fetchProfileData();
    console.log(profileData);
    // renderHeader(profileData);
    // renderLanguages(profileData);
    // renderPortfolio(profileData);
    // renderExperience(profileData);
    // renderSkills(profileData);
})()